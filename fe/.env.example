# 백엔드 API 주소.
# 비워두면 프론트가 mock 데이터(src/api/mock)로 동작합니다 — BE 없이도 화면이 전부 돌아갑니다.
# 친구분 BE가 뜨면 여기에 주소를 넣으세요. 예: http://localhost:8000
VITE_API_BASE_URL=

# true로 두면 VITE_API_BASE_URL이 있어도 강제로 mock을 씁니다 (발표 데모용 안전장치).
VITE_FORCE_MOCK=false
